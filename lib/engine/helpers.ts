// lib/engine/helpers.ts
// Hier kommen später Hilfsfunktionen rein (P6-S04ff).

import type { MarchPayload } from "./types";

export function normalizeMarch(march: MarchPayload): MarchPayload {
  // Placeholder – wird später durch echte Normalisierung ersetzt.
  return march;
}
